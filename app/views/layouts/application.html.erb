<!DOCTYPE html>
<html>
  <head>
    <title> <%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    "application", media: "all",
                                              "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "alert alert-#{key}") %>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
    <div class="container">
      <% if !Rails.env.developement? %>
<!-- Changed < % = for < % in the following 3 statements
     so that the test with micropost count is acurate -->
        <% link_to "Rails API: request", "http://api.rubyonrails.org/v4.0.0/classes/ActionDispatch/Request.html", target: "_blank" %><br>
        <% request.cookie_jar() %><br>
        <% request.ip() %>
      <% end %>
    </div>
  </body>
</html>
